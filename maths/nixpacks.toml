[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "apt-get update && apt-get install -y tesseract-ocr libtesseract-dev poppler-utils libglib2.0-0 || true",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn api:app --host 0.0.0.0 --port $PORT"

